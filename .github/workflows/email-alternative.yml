name: Daily Email Reminder (Alternative)

on:
  schedule:
    - cron: "30 4 * * *" # 10:00 AM IST
  workflow_dispatch:

jobs:
  send-email:
    runs-on: ubuntu-latest
    steps:
      - name: Send Email via Gmail SMTP
        run: |
          # Using Gmail's free SMTP
          EMAIL="${{ secrets.GMAIL_EMAIL }}"
          PASSWORD="${{ secrets.GMAIL_APP_PASSWORD }}"
          TO_EMAIL="$EMAIL"  # Send to yourself
          
          MESSAGE="Subject: Daily Reminder $(date '+%Y-%m-%d')
          
          Good morning! 🌅
          
          This is your daily reminder for $(date '+%A, %B %d, %Y').
          
          Have a great day! 💪"
          
          echo "$MESSAGE" | curl --ssl-reqd \
            --url 'smtps://smtp.gmail.com:465' \
            --user "$EMAIL:$PASSWORD" \
            --mail-from "$EMAIL" \
            --mail-rcpt "$T